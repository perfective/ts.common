{
    "name": "root",
    "private": true,
    "scripts": {
        "lint": "npm run build:eslint && lerna run --stream --no-bail --no-prefix lint",
        "build": "lerna run --stream build",
        "build:eslint": "lerna run --scope @perfective/eslint-config --stream build",
        "test": "lerna run --stream --no-bail --no-prefix test"
    },
    "devDependencies": {
        "@perfective/eslint-config": "file:packages/eslint-config",
        "@perfective/maybe": "file:packages/maybe",
        "@types/jest": "^25.2.1",
        "@typescript-eslint/eslint-plugin": "^2.28.0",
        "@typescript-eslint/parser": "^2.28.0",
        "eslint": "^6.8.0",
        "eslint-import-resolver-typescript": "^2.0.0",
        "eslint-plugin-deprecation": "^1.0.0",
        "eslint-plugin-import": "^2.20.2",
        "jest": "^25.3.0",
        "lerna": "^3.20.2",
        "ts-jest": "^25.3.1",
        "tslint": "^6.1.1",
        "typescript": "^3.8.3"
    }
}
